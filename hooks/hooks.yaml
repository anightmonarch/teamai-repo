hooks:
  - id: block-rm-rf
    description: 阻止危险的 rm -rf 命令
    event: PreToolUse
    matcher: Bash
    command: 'bash -lc "echo "[teamai] Blocked dangerous rm -rf" >&2; exit 1"'
    tools: [claude, codex]

